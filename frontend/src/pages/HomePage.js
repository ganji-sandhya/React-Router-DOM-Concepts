import classes from '../components/ErrorPage.module.css';
const HomePage = () => {
    return (
        <>
            <p className={classes.content}>Welcome to Events</p>
        </>
        );
};

export default HomePage;